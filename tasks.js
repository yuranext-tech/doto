// ============================================================
// DOTO — Задачі
// Редагуй цей файл вручну. Не використовуй AI для генерації.
// Хороша задача: один об'єкт, сенсорна деталь, без оцінки, відкритий результат.
// Додавай 2-3 на тиждень — коли щось помітив у місті.
// ============================================================

const TASKS = {

    // Перший візит, пара. Вхід через абсурд і сміх.
    // Правило: «Знайди X, який/яка [несподівана людська якість]»
    absurd: [
        'Знайди жабку, яка дивиться як твій колишній о 3 ночі.\nСфоткай крупним планом. Надішли сюди. Без слів.',
        'Знайди дерево, яке ходить до психолога вже третій рік.\nСфоткай так, щоб було видно його проблему.',
        'Знайди лавку, яка явно переживає розставання.\nСфоткай. Без підпису.',
        'Знайди камінь, який виглядає так, ніби щойно прийняв важливе рішення.\nСфоткай. Надішли.',
        'Знайди тінь, яка поводиться підозріло.\nСфоткай так, ніби це доказ.',
    ],

    // Візити 2-4, пара або соло. Ядро — присутність.
    // Правило: «Знайди місце/щось, де [сенсорний досвід]»
    ambient: [
        'Знайди місце, де хочеться говорити шепотом.\nПостій там 30 секунд.\nНадішли один емодзі, який описує повітря.',
        'Знайди щось, що виглядає так, ніби воно чекає вже дуже давно.\nСфоткай. Надішли. Без слів.',
        'Знайди місце в парку, де тихіше ніж скрізь.\nПостій хвилину. Надішли фото того, що побачив.',
        'Знайди щось, що виглядає інакше після дощу.\nСфоткай. Без пояснень.',
        'Знайди місце, де світло падає не так, як скрізь.\nПостій там. Надішли фото тіні, а не джерела.',
    ],

    // Візит 5+, тільки пара. Тиха координація без слів.
    coordination: [
        'Знайди тінь, яка схожа на тварину. Не кажи вголос, яку.\nСфоткай.\nЯкщо ви обидва вгадали одне й те саме — отримуєте несправжнє очко.',
        'Знайдіть разом щось, що явно не на своєму місці.\nОбидва фоткаєте. Надсилаєте сюди.',
        'Знайдіть разом найстарішу річ у цьому місці.\nКожен незалежно вирішує, яка вона. Надішліть кожен своє фото.',
    ],

    // Сольний режим. Тихе місто, власний погляд.
    city_attention: [
        'Сьогодні точка тиха.\n\nЗнайди жабку, яка не хоче, щоб її знайшли.\nСфоткай так, ніби ти її спалив.\nНадішли. Вона йде в архів.',
        'Сьогодні точка тиха.\n\nЗнайди місце, де місто звучить так, ніби про тебе забуло.\nПостій там 20 секунд.\nСфоткай щось, повз що всі проходять.',
        'Сьогодні точка тиха.\n\nЗнайди щось, що стоїть тут довше за всіх людей навколо.\nСфоткай. Надішли без підпису.',
        'Сьогодні точка тиха.\n\nЗнайди місце, де ти відчуваєш себе менш помітним.\nПостій. Сфоткай те, що перше впало в очі.',
    ],

};

function pickTask(visitCount, hasPartner) {
    if (!hasPartner) {
        const p = TASKS.city_attention;
        return { type: 'city_attention', text: p[Math.floor(Math.random() * p.length)] };
    }
    if (visitCount <= 1) {
        const p = TASKS.absurd;
        return { type: 'absurd', text: p[Math.floor(Math.random() * p.length)] };
    }
    if (visitCount <= 4) {
        const p = TASKS.ambient;
        return { type: 'ambient', text: p[Math.floor(Math.random() * p.length)] };
    }
    const useCoord = visitCount >= 5 && Math.random() < 0.5;
    const p = useCoord ? TASKS.coordination : TASKS.ambient;
    return { type: useCoord ? 'coordination' : 'ambient', text: p[Math.floor(Math.random() * p.length)] };
}

module.exports = { TASKS, pickTask };

// ============================================================
// Як писати хороші задачі
//
// ✓ Один об'єкт — не «5 речей», а «одна жабка»
// ✓ Сенсорна деталь — не «красиве», а «де тихіше ніж скрізь»
// ✓ Без оцінки — не «найдивніше», а «яке ходить до психолога»
// ✓ Відкритий результат — «сфоткай так, щоб було видно проблему»
// ✓ Пишеться коли щось побачив у місті — не за столом
//
// ✗ Не генеруй через AI
// ✗ Не додавай більше 3 за раз
// ✗ Не пояснюй задачу всередині задачі
// ============================================================
